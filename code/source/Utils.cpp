#include "Utils.hpp"

int example::Utils::polygon_clipper(const Point4i* vertices,  int* first, const int* last, const int viewport_width, const int viewport_height, Point4i* clipped_vertices)
{
	for (const int* index_iterator = first; ++index_iterator < last; )
	{
	}

	return 0;
}
